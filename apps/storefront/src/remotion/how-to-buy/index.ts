// Separate Remotion entry for the customer "how to buy" videos, rendered by hand
// (see README.md); it is not part of the storefront build.
import { registerRoot } from "remotion";
import { HowToBuyRoot } from "./root";

registerRoot(HowToBuyRoot);
