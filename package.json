{
  "name": "online-saler",
  "version": "0.1.0",
  "private": true,
  "description": "Second-hand Digital Platform monorepo.",
  "scripts": {
    "check:repo": "node scripts/check-repo.mjs",
    "ci": "npm run check:repo"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
